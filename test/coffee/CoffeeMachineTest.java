package coffee;

import org.junit.Test;

public class CoffeeMachineTest {
  @Test
  public void testMakeEspresso() throws Exception {
//    CoffeeMachine coffeeMachine = new CoffeeMachine();
//    coffeeMachine.beansContainer = mock(BeansContainer.class);
//    when(coffeeMachine.beansContainer.dispenseBeans(7)).thenReturn(new Beans(7));
//    coffeeMachine.boiler = mock(Boiler.class);
//    when(coffeeMachine.boiler.boil(30)).thenReturn(coffeeMachine.boiler);
//    when(coffeeMachine.boiler.getHotWater()).thenReturn(new Water(30, 97));
//
//    coffeeMachine.mill = mock(Mill.class);
//    when(coffeeMachine.mill.mill(any(Beans.class))).thenReturn(new GroundBeans(7));
//
//    Coffee coffee = coffeeMachine.makeCoffee(new EspressoReceipt());
//    Assert.assertEquals(30, coffee.milliliters);
//    Assert.assertTrue(coffee.celsiusDegrees >= 88);
  }
}